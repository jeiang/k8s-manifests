apiVersion: v1
kind: Service
metadata:
  name: {{ include "bill-splitter.fullname" . }}
  labels:
    {{- include "bill-splitter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "bill-splitter.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
