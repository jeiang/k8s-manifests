apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "blocky-dns.fullname" . | quote }}
  labels:
    {{- include "blocky-dns.labels" . | nindent 4 }}
data:
  config.yml: |
{{ include "blocky-dns.config" . | indent 4 }}
